// Not currently used — the "Suggest a message" AI feature runs on
// OpenAI's API instead (see lib/openai.js). Left in place rather than
// deleted since this sandbox can't remove files; safe to delete by hand
// if you'd like to tidy it up.
